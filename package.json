{
  "name": "shrike-ci",
  "private": true,
  "type": "module",
  "workspaces": ["core", "action"],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.0"
  }
}
